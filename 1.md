# Analysys for applied mathematics exam

Welcome, this should be the file introducing you to this math adventure, are you ready?

In this file I will just give you a short description for each of the steps that you need to follow when studying a function (analisys).

## Steps

Steps are fundamental. You should perfectly memorize this little list of 7 steps.

1. Domain
2. Axis Intescections
3. Symmetry
4. Sign
5. Limits and Asinthotes
6. First derivatives
7. Second derivatives

**NOTE**: after each step, when possible, you are supposed to draw what you found on the drawing.
The drawing should be on the top right of the page.

---

### Domain

- Definition: The domain of a function (usually written as an interval) represents all possible values for $x$ that return a real value for $f(x)$.

- Basically: if for some specific values of $x$ the function does not exist in $\mathbb{R}$, the domain is not all $\mathbb{R}$ but $\mathbb{R} -$ {all those values of x}.

**How you actually do it**: relevant situations
1. $\sqrt[n]{g(x)}$ with $n \in \mathbb{N}$ and $n$ even
2. $g(x)$ as divisor
3. $log(g(x))$ with $n \in \mathbb{R}$ and $n$ positive

$lim_{x \to 0} \frac {1} {x^2}$
